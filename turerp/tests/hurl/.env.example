# Testuser password used by the hurl smoke suite.
# Must match a real DB user. Default seed: tenant_id=1, username=testuser.
# Override per-env by passing a different value to run-all.sh.
TURERP_TEST_PASSWORD=TestUser123!Pass
